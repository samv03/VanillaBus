# vanillabus-engine

Python package for the VanillaBus CAN engine.

T1 is an installable scaffold only. SocketCAN I/O, DBC decode, and live IPC
are not implemented here yet.

## Install (editable)

From the repository root:

```bash
python3 -m pip install -e engine/
```
